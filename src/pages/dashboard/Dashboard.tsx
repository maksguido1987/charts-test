import { ChartsList, ControlPanel, Layout } from "../../components";

const Dashboard = () => {
  return (
    <Layout>
      <ControlPanel />
      <ChartsList />
    </Layout>
  );
};

export default Dashboard;
